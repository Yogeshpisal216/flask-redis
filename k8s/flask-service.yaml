apiVersion: v1
kind: Service
metadata:
  name: flask-service
  namespace: flask-redis
  
spec:
  type: NodePort
  selector:
    app: flask-backend
    version: green
  ports:
    - port: 5000
      targetPort: 5000
      nodePort: 30080

